import { ComingSoon } from '@/components/shared/coming-soon';
import { t } from '@/lib/translations';

export default function WeighSlipsPage() {
  return <ComingSoon title={t.pages.weighSlips.title} description={t.pages.weighSlips.description} />;
}
