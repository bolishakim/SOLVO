import { ComingSoon } from '@/components/shared/coming-soon';
import { t } from '@/lib/translations';

export default function HazardousSlipsPage() {
  return <ComingSoon title={t.pages.hazardousSlips.title} description={t.pages.hazardousSlips.description} />;
}
